# D3 · PWM 调光

> 场景：PWM 调光呼吸灯 demo（F 仓 `labs/w1/D3-pwm/`，**留例**——Wokwi 弃跑决策 2026-08-08）。日期：2026-08-07/11。体系：pair-learning v2（D 级三件套）

**一句话本质**：占空比 = 亮度旋钮（平均光能 = 满亮 × 占空比），频率 = 闪烁消除器（视觉暂留下限 ~60Hz）；由 LEDC 硬件外设生成（CPU 只下单）。

**讲解**：1% ≈ 灭但**0% 才真灭**；20Hz < 视觉暂留 → 频闪（头晕/相机频闪带，IEEE 1789 行业指标）。调光三选：PWM 恒流（色温稳/效率 90%+/EMI 可修=工程问题）vs 模拟调流（色温漂/损耗大=原理问题）——**选"原理级优点+工程级缺点"的路**。分辨率：编码均匀 × 人眼对数感知（韦伯-费希纳）= 低端跳变可见；目标 = **感知均匀**（gamma/对数曲线：软件 LUT / DALI 对数 / 硬件 16bit）。

**类比**：ledcWrite（外设 offload）= webhook 式（注册一次硬件自动执行）；digitalWrite = 轮询式（CPU 每周期亲自参与）——呼应 D2 轮询 vs 中断（画像库，2026-08-11 追加）。CPU 稀缺，能交给硬件的绝不自己干。

**关键点**：
- 两个独立维度：占空比→亮度，频率→闪烁，别混
- 1% ≈ 灭但 0% 才真灭
- 调光三选：EMI 能修（工程问题），色温不能修（原理问题）
- 8bit 低亮度跳变 = 编码均匀 × 人眼对数感知；目标 = 感知均匀（gamma/对数曲线）
- ledcWrite：CPU 参与一次（写占空比/CCR），不参与每周期翻转
- 16 通道、频率/分辨率独立可配 = W3 调光驱动底层肌肉

**项目落点**：控制层调光驱动底层机制（架构图"控制层深挖"节）；直接迁移照明中期目标（W3 PWM 恒流调光）。
**微题**：D3-quiz.md（批次 3）。
**钩子**：16 通道 LEDC = W3 调光驱动底层肌肉（⏳ W3 揭晓，W1-总览.md 钩子池 #2）。

## 概念回链

- D1 Q4 深入一问 → 答案在此：PWM 占空比
- 频闪/调光 → 智能照明行业经验（LED 驱动器指标，IEEE 1789）
- 对数感知 → 人类感知系统通用特性（音高/响度同源）
- 外设 offload → D2 中断主题同构（CPU 解放）
- 概念卡 → `../study-cards.md#D3`

## 自评（2026-08-11）

- 跑通：**留例**（Wokwi 弃跑）｜ 理解 ✅（Q1-Q5 全答）｜ 概念卡：已入库 ✅ ｜ 架构：控制层已填 ✅
